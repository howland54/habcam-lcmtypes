package marine_sensor;

struct MarineSensorGPS_t {
    int64_t  utime; // microseconds since 1970-01-01T00:00:00
    double   latitude; // decimal degrees
    double   longitude; // decimal degrees

}
